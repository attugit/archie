language: c++
sudo: required
services:
  - docker

before_install:
  - docker build -t attugit/testing .

script:
  - ./waf distclean configure --mode=$BUILDTYPE build install -j1 --prefix=/usr --destdir=./build/destdir
